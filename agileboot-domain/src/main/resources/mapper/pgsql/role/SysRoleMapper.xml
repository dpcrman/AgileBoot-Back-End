<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agileboot.domain.system.role.db.SysRoleMapper">

    <!-- PGSQL 优化：使用 INNER JOIN 代替退化的 LEFT JOIN，加入 DISTINCT 与 parent_id 排序 -->
    <select id="getMenuListByRoleId" resultType="com.agileboot.domain.system.menu.db.SysMenuEntity">
        SELECT DISTINCT m.*
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
        INNER JOIN sys_role r ON r.role_id = rm.role_id
        WHERE m.status = 1 AND m.deleted = 0
          AND r.status = 1 AND r.deleted = 0
          AND r.role_id = #{roleId}
        ORDER BY m.parent_id
    </select>

</mapper>
